{
  "name": "chatelherault-rc-website",
  "version": "1.0.6",
  "description": "This repository contains a normalized, static-site structure generated from Stitch exports.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "create:user": "node scripts/create-user.js",
    "cf:user-sql": "node scripts/cloudflare-user-sql.js",
    "reel:rename": "node scripts/rename-reel-images.js",
    "reel:rename:dry": "node scripts/rename-reel-images.js --dry-run",
    "snapshot": "node scripts/create-snapshot.js",
    "verify:seo": "node scripts/verify-seo.js",
    "verify:routes": "node scripts/verify-routes.js",
    "verify": "npm run verify:seo && npm run verify:routes",
    "release:patch": "node scripts/release.js patch",
    "release:minor": "node scripts/release.js minor",
    "release:major": "node scripts/release.js major",
    "version:patch": "npm version patch --no-git-tag-version && npm run verify && npm run snapshot",
    "version:minor": "npm version minor --no-git-tag-version && npm run verify && npm run snapshot",
    "version:major": "npm version major --no-git-tag-version && npm run verify && npm run snapshot",
    "build": "echo Static site: no build step required",
    "deploy": "echo Deploy is handled by Cloudflare Pages",
    "test": "echo \"No tests configured\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Frazmcc/chatelherault-rc-website.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Frazmcc/chatelherault-rc-website/issues"
  },
  "homepage": "https://github.com/Frazmcc/chatelherault-rc-website#readme",
  "dependencies": {
    "@node-rs/argon2": "^2.0.2",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "helmet": "^8.1.0"
  }
}
